<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="64"
     height="64"
     onload="
       (function () {
         try {
           var ref = document.referrer || '';
           var current = location.hostname;

           if (!current.includes('yourdomain.com')) {
             alert('⚠ Favicon loaded from another domain!\n\nDomain: ' + current);
           }
         } catch (e) {}
       })();
     ">

  <rect width="64" height="64" fill="red"/>
  <text x="16" y="42" font-size="32" fill="white">⚠</text>

</svg>
